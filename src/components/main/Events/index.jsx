import React from 'react';

//import'./style.css';

const Events = (props)=>{
    return (
      <React.Fragment>
        <h1>Events Page</h1>
      </React.Fragment>
    );
};

export default Events ;