import React from 'react';

//import'./style.css';

const Landing = (props)=>{
    return (
      <React.Fragment>
        <h1>Landing Page dev</h1>
      </React.Fragment>
    );
};

export default Landing ;